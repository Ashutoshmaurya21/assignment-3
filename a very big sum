#include <bits/stdc++.h>

using namespace std;

int  main()
{
    int n;
     long sum=0;
    
    cin>>n;
   long array[n];
    for(int i=0;i<n;i++)
    {
        cin>>array[i];
        sum=sum+array[i];
        
    }
    cout<<sum;
        
}
